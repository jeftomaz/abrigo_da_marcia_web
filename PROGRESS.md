# PROGRESS.md

- **Fase atual: Landing por seções** (ver `ROADMAP.md` › "Fase atual" p/ ciclo de trabalho e status por seção). Header, Hero, Preview Adoção e Preview Histórias concluídos; próximas seções entram uma a uma com mockup+conteúdo do usuário.
- Scaffold inicial criado: pnpm workspace (`apps/public`, `packages/shared`), Vite + React 19 + TS, React Router (`BrowserRouter`), Tailwind CSS v4 via `@tailwindcss/vite`. Dev server e typecheck validados.
- Decisão: Fundação (Supabase/RLS/auth admin, `apps/admin`) adiada — só monta quando Adoção (primeira página com dados reais) começar. Landing não depende disso.
- Decisão: Tailwind v4 usa config CSS-first (`@theme` em `src/index.css`), não `tailwind.config.js` (v3). Tokens do design system entram nesse bloco `@theme` conforme os mockups chegarem — mesmo princípio de "tokens centralizados, nunca hardcoded no componente" do `PROJECT.md`.
- Decisão: fidelidade visual aplicada por seção/componente já na primeira construção (não deixada para uma "passada de design" posterior), pois os mockups (desktop/mobile, light/dark) já estão disponíveis.
- Header implementado (`packages/shared/src/components/Header.tsx` + `Logo.tsx`), validado visualmente contra o mockup (desktop/mobile, light/dark) via screenshot headless. Paleta oficial (exclusiva): cinza-escuro `#262626`, cinza-medio `#404040`, cinza-claro `#E6E6E6`, marca-escura `#8D0E0C`, marca `#F15A55`, marca-clara `#FBD1D0` — nunca variar. Fonte: Jost (Google Fonts), substituindo Futura do mockup.
- Logo: texto convertido em contorno (paths) pelo usuário antes da exportação — evita depender da fonte Futura (não é web-safe/licenciada pra web) estar instalada no dispositivo do visitante. Paths extraídos como componente React (`Logo`, variantes `full`/`icon`), fill via `currentColor` p/ dar cor/tamanho controláveis.
- Nav do Header: Adoção/Histórias/Recãopensa(Eventos) são `<Link>` de rota (`/adocao`, `/historias`, `/eventos`); Doação/Sobre nós/Voluntários são âncoras (`#doacao` etc.) dentro da própria Landing — ainda sem seção correspondente até essas partes serem construídas.
- Gotcha de monorepo: Tailwind v4 não escaneia `packages/shared` por padrão (fica fora da raiz do Vite de `apps/public`). Necessário `@source "../../../packages/shared/src";` em `apps/public/src/index.css` — sem isso, classes usadas em componentes compartilhados são descartadas silenciosamente (sem erro, só não aplicam estilo). Vale pra qualquer novo pacote compartilhado.
- Pendente: medidas exatas de espaçamento/tamanho do Header (padding dos pills, gap, altura) foram aproximadas visualmente a partir do screenshot — ajustar se houver redlines/specs exatos.
- Gotcha de fidelidade: `max-w-screen-xl` (1280px) no container do Header e do Hero deixava tudo "concentrado no meio" em telas largas (mockup foi feito ~1778px de largura e usa quase a largura total, não um container centralizado estreito) — trocado pra `max-w-[1920px]` nos dois. Nav do Header também ficava colado no logo (mockup tem gap grande logo→nav e nav→toggle); fix: `lg:justify-center` no nav, mantendo `justify-start` (padrão) no mobile pra não quebrar o scroll horizontal dos pills. Cap de imagem do Hero desktop (`max-w-2xl`) removido — imagem preenche a coluna inteira, mais fiel ao tamanho do mockup.
- Hero implementado (`apps/public/src/components/Hero.tsx`, usado em `Landing.tsx`), validado visualmente (desktop/mobile, light/dark) via screenshot headless. Cores de texto seguem o mesmo par `marca-escura`/`marca-clara` do Header. Chevron mobile faz scroll suave até a próxima seção irmã (fallback: rola 1 viewport, já que ainda não há próxima seção construída).
- Gotcha de layout: título+parágrafo do Hero ficavam "afastados" no desktop porque a imagem circular usava `grid-row-span-2` ao lado deles — grid infla as duas rows pra caber a imagem alta, mesmo com `gap` pequeno. Fix: sem row-span; título+parágrafo (+ imagem mobile, escondida em `lg:`) ficam num wrapper único (`lg:flex lg:flex-col lg:gap-4`), e a imagem desktop é outro item simples do grid de 2 colunas — sem tracks compartilhados, sem inflar espaçamento. A imagem aparece 2x no JSX (uma mobile-only entre título/parágrafo, outra desktop-only ao lado) porque não dá pra reordenar via CSS um único elemento pra ficar "entre" dois textos num breakpoint e "ao lado" no outro sem duplicar.
- Imagens da Landing (uma por seção futura) já estão em `apps/public/src/assets/`: `landing_abertura.jpg` (Hero, em uso), `landing_adote.jpg`, `landing_conheca.jpg`, `landing_doacao.jpg`, `landing_eventos.jpg`, `landing_historias.jpg`, `landing_voluntario.jpg` — aguardando as seções correspondentes.
- Ícones: nunca inserir por conta própria — sempre pedir o arquivo ao usuário.
- Set de ícones (39 SVGs) inserido pelo usuário em `packages/shared/src/assets/icons/` (local único, veio com um `README.md` próprio descrevendo o requisito do Design System: cada arquivo vira ícone disponível por nome, sem editar código, e deve pintar via `currentColor`). Criado `Icon` (`packages/shared/src/components/Icon.tsx`, exportado em `index.ts`) que carrega todos os SVGs eager via `import.meta.glob(..., { query: '?raw', import: 'default', eager: true })` e injeta via `dangerouslySetInnerHTML` (seguro aqui: são assets estáticos do build, não input do usuário) — uso: `<Icon name="pata" />`. Necessário adicionar `vite` como devDependency de `packages/shared` + `"types": ["vite/client"]` no seu `tsconfig.json` (só para o tipo de `import.meta.glob`, sem afetar runtime/bundle).
- Header: ícones de sol/lua trocados do SVG genérico inline para os reais do set (`half-moon`/`sun-light`) via `<Icon />`. Usuário depois atualizou os 2 arquivos SVG com o ícone completo (círculo de fundo já embutido, cores fixas via `style="fill:..."` — não usa `currentColor`); botão voltou a renderizar só o `<Icon />` puro (removida a borda/padding que eu tinha adicionado como aproximação).
- Header mobile reestruturado em 2 linhas (mockup: logo+toggle em cima, nav com scroll horizontal embaixo — antes tudo ficava numa linha só). Container principal é `flex flex-wrap` com `order` explícito por item (nav `order-2` + `basis-full` força quebra de linha; toggle `order-1` no mobile / `order-3` no `lg:`) e `justify-between` (só afeta mobile: no desktop o `nav` com `flex-1` já ocupa todo espaço livre, então não sobra o que redistribuir).
- Padrão adotado: tamanho de texto (`text-*`) e largura máxima de parágrafo (`max-w-*`) são ajustados manualmente pelo usuário direto nas classes Tailwind de cada componente (ex.: `Hero.tsx`) — sem token centralizado pra isso por enquanto. Replicado no parágrafo do `AdocaoPreview` (`max-w-4xl text-2xl`) pra manter a mesma escala tipográfica entre seções.
- Preview de Adoção implementado (`apps/public/src/components/AdocaoPreview.tsx`), validado visualmente (desktop/mobile, light/dark). Gotcha de fidelidade: essa seção **inverte** o par claro/escuro do resto do site — bg `cinza-escuro` no tema claro e `cinza-claro` no tema escuro (mockup confirma esse padrão, é a seção de contraste logo após o Hero coral). Cards usam `bg-cinza-medio` (tema claro) / `bg-white` (tema escuro, único uso de branco puro fora da paleta de 6 cores — é a superfície "elevada" do card sobre o `cinza-claro` da seção, sem token melhor disponível). Conteúdo dos cards (nomes, tags "MODELO TAG", texto descritivo) é placeholder do próprio mockup — troca por dados reais quando a página/admin de Adoção (fase 3) existir. Ícone do botão "Ver todos os cães" (`wolf-solid`) é uma aproximação visual, não confirmada pelo usuário — mesmo tratamento dado aos ícones do Header antes de ele fornecer os arquivos exatos. Título "Adote um amigo" usa a mesma escala do `<h1>` do Hero (`text-5xl lg:text-8xl`), replicando o padrão manual do usuário.
- Criado `BlobImage` (`packages/shared/src/components/BlobImage.tsx`, exportado em `index.ts`) — as 4 fotos circulares (Hero mobile/desktop + AdocaoPreview mobile/desktop) tinham `rounded-full` idêntico duplicado; unificado num componente só, e o círculo virou levemente irregular (`border-radius` assimétrico via `style`, não dá pra fazer só com token de radius do Tailwind) ecoando os "dedos" da pata no `Logo`. Valor do contorno é uma primeira aproximação — ajustar se o usuário quiser mais/menos irregular.
- Botão "Ver todos os cães" centralizado (`mx-auto w-fit`) dentro da mesma coluna dos 3 cards — fica alinhado sob os cards no desktop e centralizado na tela no mobile (mesma largura em ambos os casos).
- Comportamento de expandir card implementado só para Adoção (`apps/public/src/components/DogModal.tsx`, usado por `AdocaoPreview.tsx`): botão "Ler mais" agora abre modal (overlay `bg-cinza-escuro/80`, fecha via botão "Fechar", clique fora, ou Esc) em vez de navegar para `/adocao`. Layout empilha verticalmente no mobile e vira split horizontal (imagem 40% / conteúdo 60%) no `lg:`, sem breakpoint extra — resultado direto do `flex-col lg:flex-row`. Validado via screenshot headless (desktop/mobile, light/dark). Decisão: dos 4 formatos de card do mockup do usuário (Vertical/Horizontal Compacto, Expandido Médio/Grande — parecem servir a um sistema genérico reutilizável futuramente por Histórias), só o expandido de Adoção foi construído agora (evita abstração especulativa antes da 2ª ocorrência real).
- Modal de Adoção: tags mantidas como "MODELO TAG" genérico (mesmo placeholder do card compacto, não porte/idade/sexo reais — schema de Adoção ainda não existe, fase 3). Imagem única, sem carrossel (campo de múltiplas fotos fica para quando o schema real existir). Novo campo `fullDescription` no tipo `DogPreview`, mesmo texto placeholder para os 3 cães (mesmo padrão do `TAGS` compartilhado). Botão "Adote-me" aponta para o Google Forms real de adoção (`https://forms.gle/nLSjXJyeLGUJXZj27`, fornecido pelo usuário, mesmo formulário para todos os cães, `target="_blank"`).
- Tipografia fluida: os tokens de tamanho de texto usados nas seções (`--text-5xl/8xl`, `--text-3xl/4xl`, `--text-2xl`, `--text-sm`, `--text-tag`, este último novo — antes era `text-[10px]` solto) foram redefinidos no `@theme` (`apps/public/src/index.css`) como `clamp()` em vez de valores fixos — texto acompanha a largura da janela continuamente em vez de saltar só nos breakpoints do Tailwind. Decisão do usuário: manter só o salto mobile→desktop (em `lg`, 1024px) nos textos que já tinham essa distinção (títulos `5xl→8xl`, `h3` do modal `3xl→4xl`) — mobile flui de 360-1024px, desktop de 1024-1920px (1920 = teto do container `max-w-[1920px]`, texto para de crescer além disso). Textos que já eram do mesmo tamanho em mobile/desktop (`2xl` parágrafo, `sm` cards/botões) viraram um único clamp contínuo 360-1920px, sem salto algum. Exceção: `tag` (badge "MODELO TAG") só encolhe em telas pequenas, nunca cresce além do valor original (10px) — crescer quebrava as 3 badges pra 2 linhas dentro do card (testado e revertido). Zero mudança nos componentes (`Hero`/`AdocaoPreview`/`HistoriasPreview`) exceto a troca do `text-[10px]` por `text-tag` em 2 lugares (`PreviewModal.tsx`, `AdocaoPreview.tsx`) — o resto reaproveita as classes `text-*`/`lg:text-*` já existentes, só o valor por trás do token mudou. Validado visualmente (light/dark, larguras 360/390/700/1024/1300/1920) sem regressão nos pontos já aprovados (390 mobile, 1920 desktop renderizam os mesmos tamanhos de antes).
- Preview de Histórias implementado (`apps/public/src/components/HistoriasPreview.tsx`), validado visualmente (desktop/mobile, light/dark). 2ª ocorrência do padrão "seção com cards + modal" (a 1ª foi `AdocaoPreview`) — `DogModal` generalizado para `PreviewModal.tsx` (mesmo arquivo, props `tags`/`primaryAction` agora opcionais); `AdocaoPreview` migrado para o componente genérico, `GOOGLE_FORM_URL` movida pra lá (modal não deve mais hardcodar links de domínio específico). Layout replica o do `Hero` (bg-marca, texto marca-escura/marca-clara, imagem à direita no desktop) em vez do `AdocaoPreview` (que inverte cores e espelha a imagem) — mockup do usuário confirma esse padrão. Cards sem tags (histórias de adoção não têm porte/idade/sexo). Botão "Conheça essa história" abre `PreviewModal` sem `primaryAction` (só "Fechar") — ainda não há link pronto para `/historias` (fase 4). Botão "Ver todas as histórias" (ícone `open-book`) já aponta para `/historias`, rota sem página ainda. Conteúdo é placeholder do mockup, incluindo texto duplicado do `Hero` no parágrafo e nome "Maia" repetido em 2 dos 3 cards — replicado literalmente, a revisar quando o conteúdo real de Histórias existir (fase 4). Imagem única (`landing_historias.jpg`) reusada nos 3 cards + blob, mesmo padrão do `AdocaoPreview`.
- Ajuste no `PreviewModal.tsx`: quando há `primaryAction` (caso Adoção), a linha de botões vira `grid grid-cols-[minmax(0,1fr)_minmax(0,2.5fr)]` — Adote-me fica 2.5x a largura de Fechar, cada um flush na borda do seu lado. Gotcha: `flex: 1`/`flex: 2.5` (mesmo com `flex-basis:0%` e `min-width:0`) **não** dá proporção exata quando os itens têm padding — o motor de flex ainda infla o item menor acima da sua fatia proporcional. `grid-template-columns` com `minmax(0, Nfr)` não sofre disso e dá a razão exata. Caso sem `primaryAction` (Histórias) mantém `flex` simples, sem alteração visual.
